name: Push to AWS ECR
on:
  push:
    branches:
      -  main #TechdemoApp-GitAction

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Run Test Command
        run: |
          echo "Running tests..."
          git branch
          # Add your test commands here
